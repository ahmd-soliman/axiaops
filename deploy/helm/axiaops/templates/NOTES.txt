AxiaOps ({{ .Release.Name }}) deployed.

{{- if .Values.ingress.enabled }}
{{- if .Values.ingress.host }}
Dashboard:       http://{{ .Values.ingress.host }}{{ if .Values.ingress.tls.enabled }} (https){{ end }}
{{- end }}
{{- else }}
ingress.enabled is false -- no Ingress was created. Reach the dashboard via:
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "axiaops.fullname" . }}-dashboard 8080:80
{{- end }}

{{- if not .Values.postgres.existingSecret }}

WARNING: postgres.existingSecret is unset -- api/ingestion/migrate
have NO database connection configured and will not start. Create a Secret
with keys database-url, migration-database-url, runtime-admin-database-url
and set postgres.existingSecret to its name.
{{- end }}

{{- if not .Values.secrets.existingSecret }}

WARNING: secrets.existingSecret is unset -- ENCRYPTION_KEY is not set.
api/ingestion will fail to start outside DEV_MODE (and log a warning even
inside it). Create a Secret with at least an encryption-key key and set
secrets.existingSecret to its name.
{{- end }}
