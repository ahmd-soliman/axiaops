apiVersion: v2
name: axiaops
description: >-
  AxiaOps tenant stack (api, ingestion, dashboard, valkey) for a single
  environment — one chart install per environment. Postgres is external
  (see postgres.existingSecret in values.yaml) rather than a
  chart-bundled database.
type: application
version: 0.3.6
appVersion: "0.2.0-beta.8"
