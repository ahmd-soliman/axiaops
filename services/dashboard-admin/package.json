{
  "name": "dashboard-admin",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "AxiaOps platform admin plane UI — staff-only, served behind the admin ingress. Deliberately separate from the tenant dashboard (services/dashboard) so no staff code ships in the tenant bundle (saas-platform-admin-design §3).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test:run": "vitest run"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-router-dom": "^6.30.0"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "@testing-library/user-event": "^14.6.1",
    "@vitejs/plugin-react": "^4.3.4",
    "jsdom": "^29.1.1",
    "vite": "^6.3.3",
    "vitest": "^4.1.5"
  }
}
