// This file is intentionally left as a stub.
//
// The app has been migrated to expo-router. The entry point is now
// `expo-router/entry` (set via "main" in package.json). All routing and
// provider setup lives in the app/ directory:
//
//   app/_layout.js                  — Root layout (QueryClient, Theme, SafeAreaView)
//   app/login.js                    — /login
//   app/(auth)/_layout.js           — Auth guard (redirects to /login if no token)
//   app/(auth)/index.js             — / (Dashboard)
//   app/(auth)/detail/[id].js       — /detail/<resource_id>?account=…&region=…&service=…
//   app/(auth)/trend.js             — /trend
//   app/(auth)/connect.js           — /connect
//   app/(auth)/settings/[accountId].js — /settings/<accountId>
//   app/+not-found.js               — 404
